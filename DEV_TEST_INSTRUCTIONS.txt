DEV ONLY — V3.1 Long Polling + Logo V4

- forceLongPolling() ก่อน getDatabase()
- ปิดและถอน Service Worker ของ DEV
- ลบ cache ที่ชื่อมี ske-truck-dev
- ไม่มี goOffline/goOnline
- ไม่มี hard reload หรือ reconnect timer
- หน้าเริ่มต้นยังเป็นโลโก้พื้นขาว

อัปทั้งชุดลง DEV แล้วเปิดหนึ่งครั้ง ปิด Chrome และเปิดใหม่ก่อนทดสอบ
ทดสอบ Wi-Fi↔4G, Airplane Mode, background/resume และอ่าน/เขียนข้อมูลจริง
ห้ามอัป Production จนกว่า DEV จะผ่าน

เวอร์ชัน: v2026.07.26-dev-v3.1-long-polling-no-sw-v4
